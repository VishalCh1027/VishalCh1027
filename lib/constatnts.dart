import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

const primaryColor = Colors.white;
const secondaryColor = Colors.blue;
const bgColor = Color.fromARGB(1, 35, 38, 41);

const Color kPrimaryColor = Colors.white;
const Color textColor = Color.fromRGBO(39, 50, 80, 1);
const Color kDangerColor = Color.fromRGBO(247, 71, 104, 1);
const Color kCaptionColor = Color.fromRGBO(98, 108, 139, 1);
const Color kSuccessColor = Color.fromRGBO(97, 201, 200, 1);

TextStyle? pageTitle = GoogleFonts.signika(
    fontWeight: FontWeight.w600, fontSize: 15, color: Colors.grey[700]);

const defaultPadding = 16.0;
